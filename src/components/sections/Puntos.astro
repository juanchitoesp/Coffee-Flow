<section id="puntos">
  <div class="section-head reveal">
    <div class="section-num">05 — Logística</div>
    <h2 class="section-title">Puntos de <em>encuentro</em>.</h2>
  </div>

  <div class="puntos-grid">
    <div class="map-embed reveal">
      <iframe src="https://www.openstreetmap.org/export/embed.html?bbox=-73.99%2C4.71%2C-73.94%2C4.74&layer=mapnik&marker=4.7223%2C-73.9685" loading="lazy"></iframe>
    </div>

    <div class="puntos-info reveal-stagger">
      <div class="puntos-address">
        <div class="puntos-address-label">Dirección · Cundinamarca</div>
        <h3 class="puntos-address-name">Hacienda <em>El Roble</em></h3>
        <p class="puntos-address-detail">Vía La Calera — Sopó, km 7<br>La Calera, Cundinamarca · Colombia<br><br>40 min desde el norte de Bogotá</p>
      </div>

      <div class="punto-item">
        <div class="punto-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="9" r="3"/><path d="M12 2C7.5 2 4 5.5 4 9.5c0 5 8 12.5 8 12.5s8-7.5 8-12.5C20 5.5 16.5 2 12 2z"/></svg></div>
        <div class="punto-name">Punto de registro</div>
        <div class="punto-loc">Casa principal</div>
      </div>

      <div class="punto-item">
        <div class="punto-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="11" width="18" height="8" rx="1"/><path d="M5 11l2-5h10l2 5"/><circle cx="7.5" cy="16.5" r="1"/><circle cx="16.5" cy="16.5" r="1"/></svg></div>
        <div class="punto-name">Zona de parqueo</div>
        <div class="punto-loc">Entrada sur</div>
      </div>

      <div class="punto-item">
        <div class="punto-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2 C 8 6, 6 10, 6 14 a 6 6 0 0 0 12 0 c 0 -4 -2 -8 -6 -12 z"/></svg></div>
        <div class="punto-name">Hidratación</div>
        <div class="punto-loc">Pérgola central</div>
      </div>
    </div>
  </div>
</section>

<style>
  .puntos-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: start; }
  .map-embed { aspect-ratio: 4 / 5; border-radius: 4px; overflow: hidden; border: 1px solid var(--line); filter: contrast(1.05) saturate(0.9) sepia(0.15); }
  .map-embed iframe { width: 100%; height: 100%; border: 0; display: block; }
  .puntos-address { margin-bottom: 50px; padding-bottom: 30px; border-bottom: 1px solid var(--line); }
  .puntos-address-label { font-family: 'JetBrains Mono', monospace; font-size: 11px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--ink-mute); margin-bottom: 12px; }
  .puntos-address-name { font-family: 'Fraunces', serif; font-size: 38px; font-weight: 500; line-height: 1.05; letter-spacing: -0.02em; margin-bottom: 8px; }
  .puntos-address-name em { font-style: italic; font-weight: 300; color: var(--olive); }
  .punto-item { display: grid; grid-template-columns: 32px 1fr auto; gap: 20px; padding: 20px 0; border-bottom: 1px solid var(--line); align-items: center; }
  .punto-icon { width: 32px; height: 32px; display: grid; place-items: center; color: var(--terracotta); }
  .punto-name { font-family: 'Fraunces', serif; font-size: 19px; font-weight: 500; }
  .punto-loc { font-family: 'JetBrains Mono', monospace; font-size: 11px; color: var(--ink-mute); text-transform: uppercase; letter-spacing: 0.08em; }
  @media (max-width: 980px) { .puntos-grid { grid-template-columns: 1fr; gap: 40px; } }
</style>